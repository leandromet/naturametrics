# Naturametrics — runtime dependencies
# See doc/08-dev-environment.md §3 for what is deliberately absent and why.

# Core
reflex==0.8.27              # same minor as Yvynation, so its component API matches
python-dotenv==1.2.2  # >=1.2.2 fixes CVE-2026-28684 (set_key symlink handling);
                       # unexploitable here (this app only calls load_dotenv,
                       # never set_key) but no reason to leave it flagged

# Earth Engine
earthengine-api>=1.7.4
google-auth>=2.27.0
google-auth-oauthlib>=1.2.1

# Abuse control (services/abuse_control.py) — per-IP/per-session rate limiting
# and access logging for the bulk export, backed by its own GCS bucket.
google-cloud-storage>=2.10.0

# Data
pandas>=2.2.0
numpy>=2.0.0
# No spreadsheet library: services/ods.py writes ODS by streaming XML into a
# ZIP. odfpy was measured at 42 s for 60 000 rows and degrading; the streaming
# writer does 600 000 in 3.9 s. See that module's docstring.

# Viz
plotly>=6.1.1

# Geospatial (runtime)
shapely>=2.0.2
pyproj>=3.6.0
# KML parsing for the drawn/uploaded study region (services/region_geometry.py)
# — defusedxml blocks XML-entity/billion-laughs attacks a plain ElementTree
# parse would be vulnerable to on an untrusted uploaded file.
defusedxml>=0.7.1

# Utilities
requests>=2.31.0
pydantic>=2.5.0
